apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: zot
  namespace: flux-system
spec:
  type: oci
  interval: 10m
  url: oci://zot.schober.dev/charts
  secretRef:
    name: flux-zot-oci-credentials
